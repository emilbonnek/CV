---
import { twMerge } from "tailwind-merge";

interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<ul
  class={twMerge(
    "m-0 flex list-none flex-col justify-center gap-[2.5mm] p-0",
    className,
  )}
>
  <slot />
</ul>
