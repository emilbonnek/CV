---
import { twMerge } from "tailwind-merge";

interface Props {
  class?: string;
  href: string;
}
const { class: className, href } = Astro.props;
---

<a
  class={twMerge(className, "text-inherit no-underline hover:underline")}
  href={href}><slot /></a
>
