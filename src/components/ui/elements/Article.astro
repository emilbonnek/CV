---
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<article class={className}><slot /></article>

<style is:global>
  article {
    display: flex;
    flex-direction: column;
    gap: 0.75mm;
  }
</style>
