---
import { twMerge } from "tailwind-merge";

interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<hr class={twMerge("my-0 block w-full border-t-[0.75mm]", className)} />
